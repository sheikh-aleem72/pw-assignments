const num = 4;
const result =
  num > 0
    ? "Number is Positive"
    : num < 0
    ? "Number is Negative"
    : "Number is zero";
console.log(result);

const num2 = 0;
const result2 =
  num2 > 0
    ? "Number is Positive"
    : num2 < 0
    ? "Number is Negative"
    : "Number is zero";
console.log(result2);

const num3 = -3;
const result3 =
  num3 > 0
    ? "Number is Positive"
    : num3 < 0
    ? "Number is Negative"
    : "Number is zero";
console.log(result3);
