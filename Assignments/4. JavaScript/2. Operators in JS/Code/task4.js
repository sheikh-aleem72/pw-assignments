const age1 = 18;
let isEligible = age1 < 18 ? "You cannot vote" : "You can vote";
console.log(isEligible);

const age2 = 10;
isEligible = age2 < 18 ? "You cannot vote" : "You can vote";
console.log(isEligible);
